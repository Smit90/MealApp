class Category {
  constructor(id, title, color, secondaryColor) {
    this.id = id;
    this.title = title;
    this.color = color;
    this.secondaryColor = secondaryColor;
  }
}

export default Category;
